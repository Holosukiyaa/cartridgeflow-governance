# CartridgeFlow 外置协议卡片治理 TODO

> 状态：责任路由 P0、clean-v1 四层产品投影、权威协议源发布和正式 Registry/Base v4 切换均已完成。本文件位于治理仓，记录最终验收范围。
>
> 本文件取代此前“用一套四层总协议覆盖整个项目，再让全部产品实现和检测服从该协议”的治理计划。旧计划中的 worker 顺序、全项目协议覆盖率、统一协议数据库、产品只读协议副本和协议树浏览器不再是执行目标。
>
> 旧 `MENTOR_WORKERS.md` 已退出有效治理入口并移入工作区外归档，其中的 worker-001 至 worker-010 不得继续启动。

## 一、停止旧治理计划

- [x] 停止把 CartridgeFlow 的所有内部事实纳入一套全局协议。
- [x] 停止要求 Base、Intent Studio、Capability Workshop、发行链路和 DR 同时迁移到一个统一协议版本。
- [x] 停止让数据合同从属于大型协议层级并继承其发布生命周期。
- [x] 停止以“四层、固定模块数、固定合同数、规则覆盖率 100%”作为整个项目的完成标准。
- [x] 停止继续执行旧 `MENTOR_WORKERS.md` 中的 worker-001 至 worker-010 链路。
- [x] 停止把协议浏览器继续扩建为全项目协议树浏览器。
- [x] 旧产品只读协议库和协议重建草稿不再扩张；`protocol-source` 只发布真正的跨边界 clean-v1 产品合同。
- [x] 当前正在进行的协议重建产物不得自动成为产品实现前置条件；必须重新判断其中哪些是真正的跨边界产品合同。

## 二、新治理目标

建立一套位于 CartridgeFlow 产品之外、可以整体拆除的治理脚手架：

```text
CartridgeFlow 产品仓库
        ↑ 只读扫描
外置治理扫描器
        ↓
源码事实与依赖图
        +
协议卡片、边界卡片、规则和检测器
        ↓
全局治理目录
        ↓
AI 上下文包 / 检测诊断 / 卡片浏览器 / 发布门禁
```

核心目标：

- [x] 将原来的大型协议拆成可以独立修改和独立验证的协议卡片。
- [x] 按项目领域建立“一个楼层一张主要协议卡”的逻辑治理结构。
- [x] 将跨楼层事实放入独立边界卡，不隐藏在任一楼层卡中。
- [x] 使用全局治理目录组合所有卡片、作用域、关系、检测器和场景。
- [x] 让总管 AI 只读取精简目录，再按任务加载相关卡片。
- [x] 让工作 AI 在违反规则时收到确定性的检测失败和相关卡片定位。
- [x] 保持治理系统对产品的单向依赖，产品不得依赖治理系统。

## 三、不可违反的边界

- [x] 治理系统必须位于独立兄弟仓库或独立外置目录，不成为 CartridgeFlow 产品模块。
- [x] CartridgeFlow、DR 和发布物不得导入治理 SDK、治理数据库或卡片运行时。
- [x] 不为了治理而向产品对象、API、卡带或运行数据增加规则 ID、卡片 ID 或治理字段。
- [x] 治理系统可以读取产品源码、配置、测试和构建产物；产品不得反向读取治理系统。
- [x] 卡片浏览器必须只读，不成为第二个未经校验的卡片编辑器。
- [x] 任意治理规则必须由确定性检测器判定，不能由向量相似度或 AI 主观判断决定。
- [x] 向量检索只用于发现可能相关的卡片，不能决定作用域、依赖、合规或验收。
- [x] 不在第一阶段物理拆分主仓库；先完成逻辑楼层和真实依赖扫描。
- [x] 不在新治理闭环验收前删除现有协议、检测脚本和恢复材料。
- [x] 删除或关闭整个治理系统后，产品必须仍能构建、启动、运行和交付。

## 四、治理单元

### 4.1 协议卡片

协议卡片是规范性治理单元，不是普通知识摘要。每张卡片至少包含：

- [x] 稳定卡片 ID、类型、标题、摘要、状态和独立修订号（知识卡和任务卡明确为无修订的当前内容）。
- [x] 负责内容和明确的非负责内容。
- [x] 精确作用域及排除作用域。
- [x] 输入、输出、上游、下游和允许依赖。
- [x] 规范规则、严重级别和失败含义。
- [x] 已审核检测器绑定。
- [x] 成功场景、失败场景和验收证据要求。
- [x] 来源路径和最后验证的目标 Git 提交。

知识卡是某一楼层、某一精确作用域的可复用当前知识，类似更深入的 `AGENTS.md`。它说明目的、稳定概念、源码导航、工作模式和易错点；不保存修订链、时间线、工作日志或“某时做了什么”，也不拥有规范规则或检测器绑定。

### 4.2 卡片类型

- [x] `constitution`：少量、稳定、全项目适用的不变量。
- [x] `floor`：一个可独立理解、修改和检测的项目领域。
- [x] `boundary`：两个或多个楼层之间的产品交接事实。
- [x] `knowledge`：面向一个楼层和精确作用域的无历史可复用知识，不直接产生强制规则。
- [x] `task`：一次施工的目标、范围、上下文、检查和停止条件。

### 4.3 全局治理目录

全局目录只保存索引和关系，不复制所有卡片正文。它必须能够回答：

- [x] 每个关键路径由哪张楼层卡负责。
- [x] 哪些边界连接哪些楼层。
- [x] 卡片之间有哪些依赖、生产、消费和影响关系。
- [x] 每条强制规则绑定哪个检测器。
- [x] 一次 Git 变更影响哪些卡片、消费者、检测器和端到端场景。
- [x] 哪些关键路径未覆盖、重复覆盖或发生所有权冲突。
- [x] 哪些检测结果已经过期或不对应当前提交。

## 五、SQLite 分离方案

不得把治理卡片直接加入现有 `protocol-source.sqlite`。复用 SQLite 技术和发布经验，但建立独立数据库身份和 schema。

```text
CartridgeFlow-governance/
├── governance-source.sqlite
├── governance-ledger.sqlite
├── schema/
├── scripts/
├── checks/
└── .data/
    └── governance-index.sqlite
```

### 5.1 `governance-source.sqlite`

权威保存人工审核的治理内容：

- [x] `registry_metadata`
- [x] `card`
- [x] `card_revision`
- [x] `card_section`
- [x] `card_scope`
- [x] `card_relation`
- [x] `rule`
- [x] `checker`
- [x] `rule_check_binding`
- [x] `scenario`
- [x] `scenario_card_binding`

### 5.2 `governance-index.sqlite`

由权威卡片库和目标源码扫描生成，不手工编辑：

- [x] `target_revision`
- [x] `observed_artifact`
- [x] `observed_symbol`
- [x] `observed_dependency`
- [x] `scope_coverage`
- [x] `finding`
- [x] `context_chunk`
- [x] `embedding` 明确后置；当前不存在需求，且不得参与规范判定。

### 5.3 `governance-ledger.sqlite`

独立、持久、只追加，不因生成索引重建而丢失：

- [x] `route_run`
- [x] `check_run`
- [x] `rule_result`
- [x] `check_diagnostic`
- [x] `evidence_dependency`
- [x] `acceptance_result`
- [x] `knowledge_sync_event`

### 5.4 数据库原则

- [x] 权威源、生成索引与持久审计 Ledger 三库分离。
- [x] 发布使用临时数据库、完整校验和原子替换。
- [x] Ledger 的运行和同步事件禁止 UPDATE/DELETE；索引重建不改变 Ledger。
- [x] 所有关系使用外键和明确类型，不使用向量相似度代替正式关系。
- [x] 卡片和规则独立修订，不继承上层卡片版本。
- [x] 检测器只能引用治理仓库中已审核的入口，禁止执行卡片正文提供的任意命令。
- [x] 第一版使用关系查询和 FTS5；向量索引后置。

## 六、首批楼层与边界

第一批只建立能够验证模型的最小闭环：

- [x] `constitution.project`：全局依赖方向、外置治理和可拆卸性。
- [x] `floor.kernel-v060`：共同 v0.6.0 Base（保留稳定卡片 ID）。
- [x] `floor.workbench-v070`：v0.7.0 语义层与工作台。
- [x] `floor.dr-v060-sp`：v0.6.0-SP 的 DR 运行台特化。
- [x] `boundary.cartridge-handoff`：工作台生成卡带到 DR 导入运行的交界。

Intent Studio 和 Capability Workshop 已依据独立源码目录、构建与测试边界拆成楼层卡。`src/core/protocol`、`src/core/runtime` 和 `src/core/studio` 经 AST 依赖审计存在大量双向依赖，因此仍属于共同内核楼层，只用更窄的知识卡提供局部上下文，不伪装成独立楼层。

## 七、阶段 0：建立外置施工边界

- [x] 确定独立治理仓库路径和 Git 身份。
- [x] 记录被治理的 CartridgeFlow 与 DR 仓库路径、仓库身份和目标提交。
- [x] 写入“产品不得依赖治理系统”的最高级检查。
- [x] 建立治理系统启用和完全移除两种验证方式。
- [x] 保留当前协议库和旧检测器为只读参考，不在本阶段清理。
- [x] 将旧 `MENTOR_WORKERS.md` 标记为停止执行，等待按新阶段重写。

### 阶段 0 验收门

- [x] 治理仓库与产品仓库具有明确物理边界。
- [x] 产品源码和运行时没有治理依赖。
- [x] 关闭治理入口后，现有产品行为不发生变化。

## 八、阶段 1：建立卡片权威源

- [x] 从空数据库创建 `governance-source.sqlite`。
- [x] 实现卡片、作用域、关系、规则、检测器和场景的最小 schema。
- [x] 实现数据库完整性、外键、摘要和权威源验证。
- [x] 实现从临时数据库原子发布权威卡片库。
- [x] 建立稳定的摘要、目录、卡片详情和关系查询视图。
- [x] 写入首批五张卡片，但只登记已确认事实。
- [x] 为每张卡片提供至少一个有效样例和一个无效样例。

### 阶段 1 验收门

- [x] 删除任一必需卡片、规则或检测器绑定都会稳定验证失败。
- [x] 卡片可独立修订，不触发无关卡片版本变化。
- [x] 数据库中不存在现有四层总协议的强制父子结构。

## 九、阶段 2：建立源码扫描与全局治理图

- [x] 只读扫描主项目与 DR 的文件、模块、符号和依赖。
- [x] 将扫描结果写入生成的 `governance-index.sqlite`。
- [x] 根据 `card_scope` 计算文件和模块的主要所有者。
- [x] 检查关键路径未覆盖、重复覆盖和排除规则冲突。
- [x] 检查跨楼层源码依赖是否经过显式 `depends_on` 关系；真实产品交接仍由边界卡治理。
- [x] 检查卡片关系图和真实源码依赖图是否矛盾。
- [x] 根据 Git diff 计算受影响卡片和传递消费者。
- [x] 将扫描提交、扫描器版本和数据库摘要写入索引证据。

### 阶段 2 验收门

- [x] 人为增加一个非法跨楼层依赖时，扫描稳定失败（覆盖 Python AST、TypeScript Compiler API 与 Tree-sitter Go AST）。
- [x] 人为增加一个未归属关键文件时，覆盖检查稳定失败。
- [x] 同一提交重复扫描产生一致的治理事实摘要。

## 十、阶段 3：建立检测编排与诊断闭环

- [x] 为每条 blocker 和 error 规则绑定真实检测器。
- [x] 复用现有有效检测脚本，但通过外部适配器调用，不把治理逻辑写回产品。
- [x] 区分卡片源验证、楼层局部检测、边界检测和端到端场景。
- [x] 支持按路径、Git diff 和受影响卡片选择必需检测器。
- [x] 支持发布前运行全部已启用强制检测。
- [x] 为每个失败输出规则 ID、卡片 ID、文件、原因、预期、实际和相关边界。
- [x] 检测结果绑定实际卡片、scope、relation、artifact、合同、检查器、路由器、上下文编译器、目标配置、卡片闭包和检查计划，过期结果不得作为通过证据。
- [x] 全局摘要只保护源库和索引完整性检查，不让无关 Knowledge 形成全局失效域。

### 阶段 3 验收门

- [x] AI 能从一次检测失败准确定位规则、卡片、检测器输出和修复边界。
- [x] 任一必需检测失败时，统一治理入口返回非零并阻止验收。
- [x] 任一强制规则缺少检测器时，全局验证失败。

## 十一、阶段 4：建立任务卡与 AI 上下文编译器

- [x] 定义任务卡的目标、允许范围、禁止范围、必需卡片、检查和停止条件。
- [x] 根据任务目标、目标路径和 Git diff 选择主要楼层卡。
- [x] 沿显式关系加载必要的边界卡、上游卡和消费者卡。
- [x] 生成固定顺序、有限大小的 Markdown 和 JSON 上下文包。
- [x] 上下文包记录选择原因，禁止只依赖语义相似度召回。
- [x] 检测失败后只追加相关诊断卡片，不重新加载整个项目。
- [x] 为总管 AI 提供精简全局目录，不提供全部卡片正文。

### 阶段 4 验收门

- [x] 一个 DR 局部任务不会默认加载完整 v0.7 工作台内容。
- [x] 一个跨工作台和 DR 的任务会自动加载卡带边界卡。
- [x] 相同输入和目标提交生成确定性的上下文包。

## 十二、阶段 5：将协议浏览器重做为卡片浏览器

现有协议浏览器在迁移期间保留。新浏览器位于外置治理项目中，复用本机只读浏览经验；不复用 Datasette 插件耦合、现有协议表结构或写死的 `protocol-source` 页面。

- [x] 首页展示楼层、边界、失败检测、覆盖缺口和目标提交。
- [x] 提供按类型、状态、楼层和作用域浏览的卡片目录。
- [x] 卡片详情展示职责、非职责、作用域、规则、检测器、关系和证据。
- [x] 提供归属关系、依赖关系和影响关系三种独立视图。
- [x] 提供按文件、卡片、规则和检测器查询影响范围的入口。
- [x] 提供精确搜索和 FTS5 全文搜索。
- [x] 提供词面相关卡片建议并明确标记为非规范性；向量检索继续后置。
- [x] 提供任务上下文包预览和选择原因。
- [x] 浏览器始终只读，所有修改通过权威卡片发布工具完成。
- [x] 浏览器可独立启动和关闭，不占用产品运行端口。

### 阶段 5 验收门

- [x] 用户能够从全局视图进入任一楼层、边界、规则和失败诊断。
- [x] 浏览器显示的提交、关系和检测状态与索引数据库一致。
- [x] 关闭或删除浏览器不影响产品运行。

## 十三、阶段 6：扩展到完整项目

- [x] 根据首批卡片的实际大小决定是否拆分 Intent Studio 和 Capability Workshop。
- [x] 为发行、信任、设置、运行、恢复和交付增加必要的楼层或边界卡。
- [x] 只把真正跨进程、跨语言、跨仓库或进入发布物的数据保留为新治理的正式产品合同。
- [x] 内部实现细节使用局部测试和知识卡，不升级为全局产品合同。
- [x] 每增加一张卡片，同时增加作用域、关系、检测器和失败样例。
- [x] 定期检查卡片粒度，合并过细卡片并拆分超出单次上下文的卡片。
- [x] 根据真实违规和变更影响完善全局元规则，不追求纸面数量。

## 十四、全局元规则

- [x] 每个关键文件和模块必须有且只有一个主要楼层所有者。
- [x] 跨楼层源码依赖必须由显式 `depends_on` 关系允许；跨进程、跨语言、跨仓库或发布物交接必须由边界卡治理。
- [x] 边界卡必须声明生产者、消费者、输入、输出和双方检测。
- [x] blocker 和 error 规则必须绑定可执行检测器。
- [x] 检测器、卡片、场景和源码引用必须存在。
- [x] 治理关系图不得出现非法循环。
- [x] 未对应当前精确依赖足迹的通过证据视为无效；全局摘要只用于数据库完整性检查。
- [x] 向量搜索结果不得自动创建正式卡片关系。
- [x] 产品运行时不得包含治理数据库、治理代码或治理服务调用。
- [x] 全量发布验收必须包含工作台到卡带再到 DR 的真实交界场景。

## 十五、最终完成标准

只有同时满足以下条件，新的治理脚手架才算完成：

- [x] 大型总协议不再承担整个项目的内部治理职责。
- [x] 所有关键领域由独立协议卡片治理，并拥有明确作用域。
- [x] 所有跨领域事实通过边界卡治理。
- [x] 全局目录能够确定性计算覆盖、依赖、影响和必需检测。
- [x] AI 能按任务获得有限且可解释的上下文包。
- [x] 违规修改能够触发卡片对应的检测失败和诊断。
- [x] 卡片浏览器能够展示全局状态、局部卡片、关系和证据。
- [x] 治理系统、数据库和浏览器均位于产品之外。
- [x] 完全移除治理系统后，CartridgeFlow 和 DR 仍能正常构建、运行和交付。
- [x] 当前新治理的正式产品合同只保留 20 个真实边界版本；8 个内部版本降为知识，31 个旧代际版本标为历史复核且仅作为观测材料保留。

## 十六、最终交付物

- [x] 独立的 CartridgeFlow 治理仓库。
- [x] `governance-source.sqlite` 权威协议卡片库。
- [x] `governance-index.sqlite` 生成治理索引。
- [x] `governance-ledger.sqlite` 持久路由、检查、五态验收和知识同步账本。
- [x] 卡片发布、验证、扫描、影响分析和检测编排 CLI。
- [x] 首批楼层卡、边界卡、全局宪法卡和任务卡模板。
- [x] 全局覆盖、依赖、影响和检测证据视图。
- [x] 外置只读卡片浏览器。
- [x] 面向总管 AI 和工作 AI 的上下文编译器。
- [x] 可拆卸性验收报告和完整失败样例。

## 十七、实施纪律

- [x] 先完成首批五张卡片的闭环，再扩展卡片数量。
- [x] 先建立精确关系和检测，再考虑向量检索。
- [x] 每次只处理一张楼层卡或一张边界卡。
- [x] 卡片顺序固定为：确认职责 -> 声明作用域 -> 声明关系 -> 绑定检测 -> 制造失败 -> 修复并验收。
- [x] 不用卡片数量代替治理覆盖证明。
- [x] 不用全文或向量搜索结果代替依赖和影响计算。
- [x] 不用浏览器展示正常代替数据库、扫描器和检测器验证。
- [x] 不在治理建设过程中顺便重构产品代码。
- [x] 不修改或回退主工作区中用户和其他 AI 的在途改动。
- [x] 任何删除、迁移或历史处理必须在新治理闭环通过后单独批准。

## 十八、责任路由与五态验收整改（2026-08-11）

- [x] 产品正式协议审计和完整 conformance 作为独立 blocker 级楼层检查运行。
- [x] 静态、楼层、边界、场景和完整验收分别记录和展示；静态通过不再等同产品通过。
- [x] 无归属或歧义输入触发保守回退，不确定性只扩大上下文和验证范围。
- [x] 公开合同按 `Contract Binding -> Boundary -> Producer + Consumer + Scenario` 反向路由。
- [x] Knowledge 保持 `revision=NULL`、不进入 `card_revision`、不拥有规则或检查器。
- [x] Knowledge 变化可通过外部 `knowledge_sync_event` 记录，卡片正文不保存时间线。
- [x] 真实工作台到 DR 场景使用 CF-CRE@2 和 clean-v1 installation request/plan/result，并覆盖公开设置、必需输入、成功交付和篡改拒绝。
- [x] 浏览器读取三库，展示五态验收、精确证据足迹、路由状态和合同闭包。
- [x] 为 Knowledge 的审核关键事实增加受限自动检测、冲突诊断、保守回退和失败测试；未结构化正文不伪称可自动判真。
- [x] 为 CF-CRE@2 非空被动 UI 投影和工具资源解析增加真实边界场景。
- [x] 建立治理仓库可审阅 Git 基线，并通过 `knowledge_sync_event` 把 AI、原因、源码锚和实际 changed paths 关联进 Ledger。
- [x] 新协议完成后已发布 `release/clean-v1`，产品锁精确绑定源提交和数据库摘要，摘要或提交不一致时完整验收仍会失败关闭。

## 十九、clean-v1 协议落地状态（2026-08-11）

- [x] Registry v4 支持单一权威源、四层发布和 75 个 `1.0.0` JSON Schema 合同，禁止旧代际迁移行混入 clean-v1。
- [x] Foundation、Authoring、Distribution、Runtime 四层均有可拆卸产品投影器、正向测试和失败关闭测试。
- [x] Base 校验器严格识别 clean-v1 正式清单；`build_clean_base_candidate` 负责生成候选，原子切换器完成预检后才覆盖正式清单。
- [x] Foundation 只接受单一 `cartridgeflow-authoritative` 来源的 v4 产品锁；旧 v3 锁会被 clean-v1 Foundation 拒绝。
- [x] Workbench 的真实 CF-CRE@2 发行物可投影 clean 安装 request/plan，安装 result 只能由实际 DR 尝试产生。
- [x] DR 在状态变化前严格解码 clean 请求、复验归档、核对 package ID，再物化和激活；成功与失败结果保持 plan 身份。
- [x] 跨仓场景验证 Python 生产者与 Go 消费者的 clean 安装等价性，并证明篡改失败不改变活动卡带。
- [x] DR 暴露实际可持久化的 17 个 Runtime 合同子集，不为缺失 `expires_at` 的 pending interaction 虚构合同。
- [x] Knowledge 卡已更新为当前事实，9/9 来源锚为 current，变化原因保存在独立 Ledger 而非卡片正文。
- [x] 权威协议源提交已发布到 `release/clean-v1`，正式 v4 Registry 锁和 clean Base 已原子切换；运行兼容目录由同一锁独立固定。
